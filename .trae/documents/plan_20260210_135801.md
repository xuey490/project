I have analyzed the frontend API structure in `madong-ui-art-main` and compared it with the current PHP backend routes. There are significant differences in URL paths and some missing endpoints. I will proceed with the following plan to align them:

### 1. Update Route Configuration (`project/config/routes.php`)
Reconfigure all routes to match the RESTful style and specific paths defined in the frontend `BaseService` and API modules.

**Authentication & Core:**
- `POST /system/login` -> `Auth::login`
- `GET /system/auth/user-info` -> `Auth::getInfo`
- `GET /system/auth/user-menus` -> `Auth::getRouters`
- `GET /system/auth/perm-code` -> `Auth::getPermCode` (New)

**System Modules (User, Role, Menu, Dept, Post, Dict, Config):**
- Standardize CRUD to match `BaseService`:
  - `GET /system/{module}` -> `index` (List)
  - `GET /system/{module}/{id}` -> `show` (Detail)
  - `POST /system/{module}` -> `store` (Create)
  - `PUT /system/{module}/{id}` -> `update` (Update)
  - `DELETE /system/{module}/{id}` -> `destroy` (Single Delete)
  - `DELETE /system/{module}` -> `batchDestroy` (Batch Delete)
  - `PUT /system/{module}/{id}/change-status` -> `changeStatus`

**Specific Features:**
- **User**: `PUT /system/user/reset-password`, `PUT /system/user/grant-role`
- **Role**: `PUT /system/role/data-scope`, `GET /system/auth/user-list-by-role-id`, `GET /system/auth/user-list-exclude-role-id`, `POST /system/auth/remove-user-role`, `POST /system/auth/save-user-role`
- **Logs**: Map `/system/logs/login` and `/system/logs/operate` to the existing log controllers.

### 2. Implement Missing Controller Methods
Update the controllers to handle the new routes and logic:
- **Auth Controller**: Add `getPermCode`.
- **User Controller**: Add `batchDestroy`, `resetPassword`, `grantRole`.
- **Role Controller**: Add `dataScope`, `authUser`, `cancelAuthUser` (adapt to new paths).

### 3. Verify and Test
- Update `verify_system_modules.php` to use the new route paths for verification.
- Ensure parameter handling (e.g., batch delete via query params) works correctly.
