<?php
// config/iplist.php

return [
    'enabled' => true,

    // 黑名单模式：白名单留空 = 不限制正常用户访问（公网站点必须留空，
    // 否则只放行白名单内 IP，会把所有公网用户挡在门外）。
    // 仅当确实要做「内网/堡垒机白名单准入」时才在此填写允许的 IP/网段。
    'whitelist' => [
        // 例：'203.0.113.10', '10.0.0.0/8'
    ],

    // 黑名单：这些 IP 或网段禁止访问（优先级高于白名单）。
    // 请填写真实需要封禁的恶意 IP；以下为格式示例，部署时请清理或替换。
    'blacklist' => [
        // 例：'1.2.3.4', '203.0.113.0/24', '2001:db8:bad::/48'
    ],
];