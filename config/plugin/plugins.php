<?php

declare(strict_types=1);

/**
 * 插件配置文件
 *
 * 管理已安装插件列表和插件扫描路径
 *
 * @package Fssphp\Plugin
 * @author  Fssphp Team
 */

return [
    /*
    |--------------------------------------------------------------------------
    | 已安装插件列表
    |--------------------------------------------------------------------------
    |
    | 记录已安装插件的状态信息。键为插件名称，值包含：
    | - enabled: 是否启用
    | - version: 安装版本
    | - installed_at: 安装时间
    |
    | 示例：
    | 'blog' => [
    |     'enabled' => true,
    |     'version' => '1.0.0',
    |     'installed_at' => '2025-01-15 10:30:00',
    | ],
    |
    */
    'installed' => [
        // 已安装的插件将在这里自动注册
    ],

    /*
    |--------------------------------------------------------------------------
    | 插件扫描路径
    |--------------------------------------------------------------------------
    |
    | 定义框架扫描插件的目录列表。可以配置多个路径，
    | 框架会依次扫描这些目录下的 plugin.json 文件。
    |
    */
    'paths' => [
        BASE_PATH . '/plugins',
    ],

    /*
    |--------------------------------------------------------------------------
    | 插件默认配置
    |--------------------------------------------------------------------------
    |
    | 所有插件的默认配置选项，可以被单个插件的配置文件覆盖。
    |
    */
    'defaults' => [
        // 是否自动加载插件服务提供者
        'autoload_providers' => true,
        
        // 是否自动加载插件路由
        'autoload_routes' => true,
        
        // 是否启用插件缓存
        'cache_enabled' => true,
        
        // 插件缓存时间（秒）
        'cache_ttl' => 3600,
    ],

    /*
    |--------------------------------------------------------------------------
    | 插件命名空间前缀
    |--------------------------------------------------------------------------
    |
    | 定义插件的默认命名空间前缀。
    |
    */
    'namespace_prefix' => 'Plugins\\',
];
